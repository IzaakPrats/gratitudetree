{
  "name": "simple-scaffold-eth",
  "version": "1.0.0",
  "description": "Contract code, next code, bridge code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-contract-local": "npx --workspace=hardhat hardhat run --network localhost scripts/deploy.ts"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/next-dapp",
    "packages/hardhat"
  ]
}
